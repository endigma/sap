[tools]
"aqua:a-h/templ" = "latest"

[tasks.templ]
run = "templ generate ."

[tasks.web]
run = "templ generate --watch --proxy=\"http://localhost:8090\" --cmd=\"go run .\""

[tasks.lint]
run = "golangci-lint run"

[tasks.test]
run = "gotestsum -- ./..."
